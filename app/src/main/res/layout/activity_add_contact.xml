<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/addcontactimage"
    android:padding="24dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <!-- 🪖 Helmet Icon -->
        <ImageView
            android:id="@+id/imgHelmet"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:src="@drawable/helmet_icon"
            android:layout_marginBottom="12dp" />

        <!-- 📛 Title -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Smart Helmet"
            android:textAlignment="center"
            android:textColor="#FFFFFF"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginBottom="8dp" />

        <!-- ➕ Subtitle -->
        <TextView
            android:id="@+id/tvSubtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Add Emergency Contact"
            android:textAlignment="center"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:layout_marginBottom="20dp" />

        <!-- 👤 Name -->
        <EditText
            android:id="@+id/etContactName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Full Name"
            android:drawableStart="@drawable/ic_person"
            android:drawablePadding="10dp"
            android:textColor="#FFFFFF"
            android:textColorHint="#FFFFFF"
            android:padding="12dp"
            android:layout_marginBottom="12dp" />

        <!-- 📞 Phone -->
        <EditText
            android:id="@+id/etContactPhone"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Phone Number"
            android:inputType="phone"
            android:drawableStart="@drawable/ic_phone"
            android:drawablePadding="10dp"
            android:textColor="#FFFFFF"
            android:textColorHint="#FFFFFF"
            android:padding="12dp"
            android:layout_marginBottom="12dp" />

        <!-- ✉️ Email -->
        <EditText
            android:id="@+id/etContactEmail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Email"
            android:inputType="textEmailAddress"
            android:drawableStart="@drawable/ic_email"
            android:drawablePadding="10dp"
            android:textColor="#FFFFFF"
            android:textColorHint="#FFFFFF"
            android:padding="12dp"
            android:layout_marginBottom="20dp" />

        <!-- 💾 Save Button -->
        <Button
            android:id="@+id/btnSaveContact"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="SAVE"
            android:background="@drawable/glow_button1"
            android:textColor="#000000"
            android:layout_marginBottom="12dp" />

        <!-- 📋 View Saved Contacts Button -->
        <LinearLayout
            android:id="@+id/layoutSavedContact"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/speed_box_background"
            android:padding="16dp"
            android:layout_marginBottom="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="📋 Currently SAVED CONTACTS"
                android:textColor="#000000"
                android:textStyle="bold"
                android:textSize="18sp"
                android:layout_marginBottom="8dp" />

            <TextView
                android:id="@+id/tvCurrentContact"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Not Available"
                android:textColor="#000000"
                android:textSize="16sp" />

            <Button
                android:id="@+id/btnReloadContact"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="🔄 Reload Contact"
                android:backgroundTint="#BBDEFB"
                android:textColor="#000000"
                android:layout_marginBottom="16dp" />


        </LinearLayout>



        <!-- 📝 Small Note -->
        <TextView
            android:id="@+id/tvNote"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="📝 Tip: Make sure the contact can respond in emergencies."
            android:textColor="#FF0000"
            android:textSize="14sp"
            android:textAlignment="center"
            android:layout_marginBottom="20dp" />

        <!-- 🙏 Thank You -->
        <TextView
            android:id="@+id/tvThanks"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🙏 Thank you for making safety a priority!"
            android:textColor="#66FFFF"
            android:textSize="14sp"
            android:textAlignment="center" />

    </LinearLayout>
</ScrollView>
